module.exports = function multiply(first, second) {
  

  n = BigInt(first);
  m = BigInt(second);
  return String(BigInt(n*m));
  

  /*let f=first.split("");
  let s=second.split("");  
  
  for (let i=length.f; i>=0; i--){
    for (let j=length.s; j>=0; j--){
      let s[j]*f[i]
  }*/
}
